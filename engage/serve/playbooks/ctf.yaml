playbooks:
  - name: ctf-web
    objective: ctf
    workflow: ctf-web
    max_tools: 6
    description: CTF web challenge workflow (recon, enum, vuln scan)

  - name: ctf-pwn
    objective: pwn
    workflow: ctf-pwn
    max_tools: 5
    description: CTF pwn challenge workflow (checksec, static analysis)
