#!/bin/sh
# volatility3 — catalog binary: volatility
set -eu
if command -v vol >/dev/null 2>&1; then
  exec vol "$@"
fi
if python3 -m volatility3.cli "$@" 2>/dev/null; then
  exit 0
fi
exec python3 -m volatility3 "$@"
