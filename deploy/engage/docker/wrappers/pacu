#!/bin/sh
set -eu
PACU="/opt/pacu"
if [ -d "$PACU" ] && [ -f "$PACU/cli.py" ]; then
  exec python3 "$PACU/cli.py" "$@"
fi
if command -v pacu >/dev/null 2>&1; then
  exec pacu "$@"
fi
exec engage-stub "$@"
